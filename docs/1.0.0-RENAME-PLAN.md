# v1.0.0: rename to Desktop AI Companion, new repo, fresh history

## Why this exists

The product pivoted. These are not pets: converted Shimeji skins overwhelmingly represent human characters,
so "pet" mislabels most of the library. The user-visible rename to **Desktop AI Companion** already landed
(product name, tray, panes, Companion Studio). This document covers the rest, which is everything that could
not be renamed while staying backward-compatible.

**The reason to do it now and not later.** Several of these names are compatibility surfaces: settings keys,
the catalog's `pets` key, on-disk directory names, the plugin ABI. Renaming any of them normally requires a
migration or a breaking major bump. A version rebase to 1.0.0 with a **mandatory clean install** makes all of
them free, exactly once. After 1.0.0 ships, the window closes.

Decisions taken with the maintainer:

| | |
|---|---|
| Repo | `bigfnj/desktop-ai-companion` (kebab-case, reads cleanly in raw URLs) |
| Depth | **Tier 3** — everything, including the plugin ABI and settings keys |
| History | **Fresh**, single initial commit. Old repo deleted after v1.0.0 verifies |
| Version | 1.9.16 → **1.0.0** |

---

## The six things that will break this if missed

1. **`MinHostVersion` on every module.** All six catalog modules declare 1.4.0 to 1.9.9. Rebase the host to
   1.0.0 and **every one of them refuses to load** — a silent, total loss of function that looks like the
   module system being broken. All seven (six + TestModule) go to `1.0.0`.
2. **Rebasing module versions down is only safe because of the clean install.** The in-app Update button
   compares the catalog's version against the installed one, so a machine holding petstudio 1.7.0 against a
   catalog offering 1.0.0 sees itself as already ahead and is never offered the update again. Nothing on
   disk survives the clean install, so no machine is ever in that state — but that is the *reason* it is
   safe, not an accident, and it stops being true the moment anyone installs 1.0.0 modules over an old tree.
3. **The MSI needs a NEW `UpgradeCode`.** 1.0.0 is a *downgrade* of 1.9.16, so with the current UpgradeCode
   the installer refuses to install over it. A new code makes it a separate product, which is correct, and
   is what forces the clean install this whole plan depends on.
4. **The catalog cannot verify itself before it is pushed.** `catalog.json` will reference
   `raw.githubusercontent.com/bigfnj/desktop-ai-companion/master/...`, which 404s until the repo exists and
   the push lands. Order: create repo → push → *then* verify URLs. Do not treat a 404 before the push as a
   defect.
5. **Attribution does not travel with the fork link.** The current repo is a fork of
   `Adrianotiger/desktopPet`; the new one deliberately is not. What satisfies the upstream licence is
   `LICENSE`, `THIRD_PARTY_NOTICES.md` and `grimoire/01-history-and-lineage.md`, all of which are tracked
   files and must be present in the initial commit. Verify, do not assume.
6. **The pet XML schema is NOT renamed.** `animations.xml` element and action names are the Shimeji
   conversion contract and 53 published companions use them. Renaming them invalidates the entire corpus.
   Same for the built-in id `eSheep`: that is a character name, not the word "pet".

---

## What is destroyed, exactly

Written down because two of the three decisions behind this plan are irreversible, and because the
interesting losses are the ones nobody thinks of until afterwards. Measured on 2026-09-03, not estimated.

### A. Destroyed by deleting `bigfnj/desktopPet` (GitHub-side, permanent)

| | |
|---|---|
| Commits | **1,162**, first dated **2015-12-23** |
| Tags | 41 |
| Remote branches | 9 (`feature/audio-abi`, `feature/module-sdk`, `feature/petstudio-authoring`, …) |
| Issues + pull requests | up to **#89**, all closed, with their descriptions and review threads |
| Releases | 3 (`v1.9.14`, `v1.9.15`, `v1.9.16`), 15 assets, 27 downloads |
| Contributors | **13 names**, including upstream author Adriano and ~11 third parties |
| Fork/network link | to `Adrianotiger/desktopPet` — intentional, and only a new repo can shed it |
| Actions history | every run and log |

**Only this maintainer's history is preserved, by decision.** Of the 1,162 commits, **759 reachable from
HEAD are `peshinator@gmail.com`** (2026-06-16 onward); the remainder belong to the upstream project and its
contributors (`Adriano`, `Oliver`, `ϺΛDИVTTΛH`, `Markus`, `Estecka`, `AmdKing`, `Danial Nickford`,
`Niklas Rosencrantz`, `Robin Luiten`, `dependabot[bot]`). That record is the fork's, not this project's,
and it is deliberately not carried over. No obligation follows from dropping it: attribution is discharged
by `LICENSE` and `THIRD_PARTY_NOTICES.md`, which are tracked files that carry into the new repo.

**A real win hides in here.** A **work email address** authored 10 commits on 2026-06-24 and is
**currently published**, reachable in `bigfnj/desktopPet` via the old `v1.0.0` to `v1.0.6` tags. Deleting
the repo is the only thing that removes a work identity from a public git history; rewriting it in place
would mean force-pushing rewritten tags and would still leave the old objects reachable for a while.

**Historic `v1.0.0` to `v1.0.6` tags already exist** in this repo. They are why a fresh repo, rather than a
rename plus a re-tag, is the only clean route to shipping something called 1.0.0.

**Every published URL 404s permanently.** `catalog.json`, all six `modules-dist/*.zip`, the fortune packs
under `packs/`, all 53 companion XMLs, and every release download link. Any client still pointing at them
has no fallback. Today that is one machine.

### B. Discarded by the fresh-history initial commit (even though the code survives)

- The same 1,162 commits and 41 tags, locally as well as remotely. `git blame` goes with them.
- **The commit messages.** This repo's messages carry the mutation-test records, the FIRED lines, and the
  reasoning for decisions the code cannot explain by itself. From today alone: why the tray icon was never
  actually missing, why `SHA256SUMS.txt` had to move off `Set-Content`, why the shell-parity guard is
  AST-based rather than a regex, and why "Companion Studio" was reverted once before it landed.
- ~1.3 GB of `.git`, almost all sprite history. **This one is a benefit**, not a loss.

### C. Destroyed on this machine by the clean install

| Path | Size | Contents |
|---|---|---|
| `%LOCALAPPDATA%\Programs\DesktopPet AI Edition` | 35 MB | 23 downloaded companions, installed modules |
| `%LOCALAPPDATA%\DesktopPet` | 352 KB | `settings.json` (**37 keys**, schemaVersion 2), `.bak`, per-module config (`aibrain/`) |
| `%APPDATA%\DesktopPet` | 4.7 MB | `ai-settings.json`, **`chat-history.json`**, `fortunes/` (7 items) |
| `…\DesktopPet AI Edition TESTBUILD` | 324 KB | leftover side-by-side test product |

Two of these are easy to overlook:

- **`chat-history.json` is the AI Brain conversation history.** It is small, it is not in the repo, and
  nothing recreates it. Anyone who values the record of what their companion said should copy it first.
- **`settings.json` is 174 KB because it inlines the active companion's entire `animations.xml` and its
  base64 icon.** It is not just preferences; it is the definition of the companion currently on screen,
  plus per-companion sizes, monitor pins, mutes, the autostart mix, the speaking companion, volume, audio
  device and every update stamp.

Also cleared, harmlessly: the Windows `NotifyIconSettings` entries for the old executable paths, and the
HKCU `Run` autostart value pointing into the old install directory (already handled by the legacy-name
cleanup in `StartupRegistration`).

### D. Breaks rather than disappears

The app's update check, the module update check, companion downloads and fortune-pack downloads all resolve
against the old repo until a build carrying the new URLs is installed. On this machine the window between
Phase 6 and reinstalling is the only exposure; there is no other client.

### E. Explicitly NOT affected

- `upstream` (`Adrianotiger/desktopPet`) — untouched, and never a push target.
- `bigfnj/scripts-utilities` — a separate repo, already updated and pushed.
- The auto-memory notes, which live outside the repo entirely.
- The companion XML corpus and sprite sheets, which are tracked files and carry into the new repo.

---

## Phase 0 — Preserve, before anything is destroyed

Non-destructive, do first, needed regardless. Each item maps to a loss in the section above; nothing here
is optional tidiness.

**Preserve this maintainer's record only (mitigates B)**

Scoped deliberately. The upstream project's history and its other contributors' commits are **not**
preserved — that is the fork's record, not this one's, and the decision is intentional rather than an
oversight. It costs nothing legally: the obligation is discharged by `LICENSE` and
`THIRD_PARTY_NOTICES.md`, which are tracked files carried into the new repo, not by the git graph.

- [x] `docs/HISTORY-pre-1.0.0.md` — `git log` (date, subject, body) filtered to
      `peshinator@gmail.com` only: **759 commits reachable from HEAD**, 2026-06-16 to 2026-09-03. This is
      the only carrier for the mutation-test records and the reasoning once history is discarded.
      **Assert the output contains no other author** before committing it, rather than trusting the filter.
- [x] `docs/ISSUES-pre-1.0.0.md` — titles and bodies of all issues and PRs up to #89. A fork does not
      inherit issues, so every one of these is this maintainer's. Several record why an approach was
      abandoned, which nothing else captures.
- [x] Verify `LICENSE` and `THIRD_PARTY_NOTICES.md` still name the upstream project, since the fork link
      will no longer imply it. See risk 5. This is an obligation, not a preference.

**Do NOT carry across: the work identity.** A **work email address** authored 10 commits dated 2026-06-24
and is currently **published**, reachable in `bigfnj/desktopPet` through the old `v1.0.0` to `v1.0.6` tags.
Those commits are not reachable from HEAD, so a HEAD-scoped log excludes them automatically — but assert
that explicitly when generating the history file, and treat its removal as one of the concrete wins of
starting a fresh repo rather than a side effect.

Standing rule for this project: **no employer name, work email, or work material in any tracked file**,
this document included. The only author identity that belongs in the new repo is the personal one. This
paragraph deliberately does not name the address it is warning about, which is the mistake that produced
this rule: an earlier revision of this very file spelled it out in full and was pushed to the public repo.

**Back up the machine state (mitigates C)**

- [x] Copy `%APPDATA%\DesktopAICompanion` in full. It holds `chat-history.json` and `ai-settings.json`, neither of
      which exists anywhere else and neither of which is recreated by a reinstall.
- [x] Copy `%LOCALAPPDATA%\DesktopAICompanion\settings.json`. Not for restoring — the keys are being renamed — but
      as the only record of the current per-companion sizes, monitor pins, mutes and autostart mix, so they
      can be re-entered rather than rediscovered.
- [x] Decide on the 23 companions in `…\Programs\DesktopPet AI Edition\pets` (9.5 MB). Re-downloading is the
      intended path; copy them only if that is unacceptable.
- [x] Record the three release asset hashes, for a rollback comparison.

**Confirm the escape hatch still exists**

- [ ] Re-read the Rollback note at the end of this document before starting Phase 6, not before Phase 0.

---

## Phase 1 — The rename map, reviewed before any code changes

A blind sed at this scale is how an identifier gets corrupted. Today's smaller sweep already proved it: the
same four letters appear as prose, as a settings key, as a pane routing key, as a directory name, as a
fortunes pack about actual animals, and inside a URL. **Produce this table, review it, then execute from
it.**

- [x] Enumerate the map from the code, review it, and only then execute Phase 2.

### Renamed

| Category | From | To | Count |
|---|---|---|---|
| Repo + URLs | `bigfnj/desktopPet` | `bigfnj/desktop-ai-companion` | 257 (212 generated) |
| Executable | `DesktopAICompanion.exe` | `DesktopAICompanion.exe` | 409 |
| Sidecars | `DesktopAICompanion.{dll,config,deps.json,runtimeconfig.json}` | `DesktopAICompanion.*` | — |
| Portable marker | `DesktopAICompanion.portable` | `DesktopAICompanion.portable` | 7 |
| Namespace | `namespace DesktopAICompanion[.Modules,.Plugins,.Wpf,.LocalData]` | `DesktopAICompanion.*` | 148 files |
| Packages | `DesktopAICompanion.Contracts`, `DesktopAICompanion.ModuleKit` | `DesktopAICompanion.*` | 44 / 53 files |
| ABI types | `ICompanion`, `ICompanionManager`, `ICompanionPreview`, `CompanionTypeInfo`, `CompanionCount`, `CompanionSpawned`, `CompanionPoked`, `CompanionLanded`, `CompanionIdle`, `CompanionRemoved`, `CompanionsDirectory`, `WindowUnderCompanion`, `ModulePermissions.Pets` | `ICompanion`, `ICompanionManager`, … `ModulePermissions.Companions` | — |
| Host types | `FormCompanion`, `CompanionTypeRegistry`, `CompanionCatalog`, `CompanionFreshness`, `CompanionProvenance`, `CompanionsPaneControl`, `CompanionXmlValidator`, `CompanionHost` | `…Companion…` | — |
| Settings keys | `activePetId`, `petSizes`, `petMonitors`, `mutedPets`, `autoStartPets`, `defaultSpeakingPet`, `petSoundsEnabled`, `petUpdateCheck`, `petUpdateLastCheckUtc`, `petUpdateStaleIds`, `pets` | `activeCompanionId`, `companionSizes`, … | 12 |
| Data root | `%LOCALAPPDATA%\DesktopAICompanion` | `%LOCALAPPDATA%\DesktopAICompanion` | — |
| Install dir | `Programs\DesktopPet AI Edition` | `Programs\Desktop AI Companion` | done |
| Content dir | `<install>\pets\` | `<install>\companions\` | — |
| Catalog key | `"pets": [ … ]` | `"companions": [ … ]` | — |
| Env overrides | `DESKTOP_AI_COMPANION_DATA_ROOT`, `DESKTOP_AI_COMPANION_STARTUP_TEST_KEY` | `DESKTOP_AI_COMPANION_*` | — |
| Autostart value | `Desktop AI Companion` (already), legacy cleanup keeps `DesktopPet AI Edition` | — | — |
| MSI UpgradeCode | current GUID | **new GUID** | see risk 3 |
| `MinHostVersion` | 1.4.0 – 1.9.9 | `1.0.0` | 7 modules |
| Module versions | petstudio 1.7.0, reminder 1.8.1, aibrain 1.4.0, fortunes 1.2.7, remembrance 1.1.2, blinkingled 1.0.4 | `1.0.0` | 6 catalog modules |
| Remaining prose | `CompanionXmlValidator` (23), `StartUp` (36), `ReminderModule` (9), `PetStudioWindow` (5), `FormCompanion`/`CompanionCatalog`/`RemoteCatalog`/`Animations`/`CompanionHost` (33) | companion wording | ~106 |
| Docs | `Readme.md`, `SMOKETEST.md`, `handoff.md`, `BACKLOG.md`, `grimoire/`, `docs/`, `templates/` | companion wording | many |

### Traps the enumeration found (none of these were in the first draft)

**Renames that break SILENTLY — no compiler error.** Cross-assembly reflection by type-name string. A
normal rename leaves these pointing at types that no longer exist, and the failure shows up as a self-test
skipping or a soak finding nothing, not as a build break.

| string | file |
|---|---|
| `"DesktopAICompanion.FormCompanion"` (via `GetType(..., true)`, so it **throws**) | `RuntimeHardeningSelfTest.cs:414` |
| `"DesktopAICompanion.PetStudioModule.PetStudioWindow"` | `tests/DesktopAICompanion.WindowSoak/Program.cs:41` |
| `"…PetStudioModule.AnimCapabilitySelfCheck"`, `"…PetStudioTheme"`, `"…BehaviourChainSelfCheck"`, `"…PetAnalyzer"`, `"…PetStudioPaths"` | `PetStudioModuleSelfTest.cs:78,104,174,215,304` |
| `"DesktopAICompanion.PetStudio.animations.xsd"` (embedded-resource manifest name) | `modules/PetStudio/EngineShim.cs:59` |

**Two mutex-name literals that must stay identical to each other.** `@"Global\DesktopAICompanion."` appears in the
host at `AppSettingsStore.cs:1232` and in ModuleKit at `CrossSessionLock.cs:40`. They are the same lock.
Rename both or neither; renaming one silently stops the host and its modules from seeing each other's lock.

**`Pets/` with a capital P is a live URL path segment**, not just a folder: every one of the 53 published
skins resolves through `…/master/Pets/<id>/animations.xml`, and `RemoteCatalog.cs:362` asserts that exact
segment. The directory, the assertion, the 53 catalog URLs and `Pets/pets.json` move together or not at all.
Free to rename here only because the new repo republishes the catalog from scratch.

**A case-insensitive sweep will corrupt three proper nouns.** `Adriano Petrucci` (the upstream author),
`https://esheep.petrucci.ch/` (the animations XML **target namespace**, in the XSD and every skin), and the
same host in the About dialog. "Petrucci" contains "petru". Any `pet` → `companion` replacement must be
whole-word and must exclude these explicitly.

**More persisted keys than the first draft listed.** Beyond the 12 in `AppSettingsStore`:
`AiSettings.PetName` (a PascalCase field persisted into `ai-settings.json`), Reminder's per-slot
`SlotKey(i, "pet")` producing `slot1.pet`, the resx resource keys `removepet` and `petspeech`, the four
host options-pane field ids (`petsAtStartup`, `petSounds`, `petUpdateCheck`, `defaultSpeakingPet`), and the
fortune pack **file magic headers** `#!desktop-pet-fortunes-v1` / `-v2` and
`#!desktop-pet-classifier-parity-v1`.

**`CatalogKinds.Pet = "pet"` is an ABI wire value**, not just a constant: modules pass it to
`FetchCatalogItemsAsync`. The identifier and the string can both change, but only because every module is
rebuilt in the same pass.

**`docs/fti/FTI_112.json` is a generated full-text index** containing `pet`, `pets`, `petname` and
`petrucci` as tokens. Regenerate it; never hand-edit it.

**Five separate `FakeCompanion` types.** One public in `ModuleKit/Testing/Fakes.cs:102` and four private nested
copies in the self-tests. `CompanionHost.cs:928` also declares a `DenyingCompanionManager` whose name collides with the
public ModuleKit one.

### Deliberately NOT renamed

- **`animations.xml` schema** — element and action names are the Shimeji conversion contract; 53 published
  companions depend on them. See risk 6.
- **`eSheep`** as the built-in id and display name — a character, not the concept.
- **`packs/pets.txt`** and the `pets` fortunes pack ("Pets & Animals") — that one is genuinely about animals.
- **Module ids** (`petstudio`, …) — the folder name on disk, the catalog key and the published zip URL.
  Renaming orphans every installed copy for a display string. Display names already say Companion Studio.
- **`petname`** as an animations.xml element, and the `esheep.petrucci.ch` XML target namespace. The C#
  field `AnimationXML.Petname` may be renamed; the `[XmlElement("petname")]` string may not.
- **`"AutostartPets"`** in the legacy `DesktopAICompanion.config` importer, and the legacy Run-key value name.
- **Self-test flags** (`--pettyperegistry-selftest`, `--petstudio-selftest`, …) — internal, referenced by
  `build.yml` and `run-gate.ps1`; renaming is churn with no user benefit.
- **Each module's `ModuleInfo.Version` changelog comment block.** The versions rebase to 1.0.0, but those
  comments are the only surviving record of what each module release actually changed once git history is
  discarded. Keep the block, add a `1.0.0` entry naming the rebase, and do not delete the older lines.

---

## Phase 2 — Execute, in dependency order

Serial. Each step must build before the next begins, because a half-renamed ABI will not compile and the
errors from two overlapping renames are indistinguishable.

- [ ] **1. ABI**: `DesktopAICompanion.Contracts` then `DesktopAICompanion.ModuleKit` — namespace, package id, type names. Build.
- [ ] **2. Host**: namespace, type names, assembly/exe name, settings keys, data root, `pets\` → `companions\`,
   env overrides, remaining prose. Build.
- [ ] **3. Modules** (all 7): `using` updates, ABI type names, `MinHostVersion = "1.0.0"`, prose. Build each.
- [ ] **4. Installer**: `DesktopAICompanion.wxs` product/dirs/**new UpgradeCode**/`util:CloseApplication` target exe/
   shortcut names; `packaging/runtime-files.txt`; `Test-MsiSurface.ps1` assertions.
- [ ] **5. Packaging + CI**: `New-ContentCatalog.ps1` base URL, `New-ModulePublish.ps1`, `Invoke-Signtool.ps1`,
   `Normalize-MsiDeterminism.ps1`, `build.ps1`, `build-installer.ps1`, `build.yml`, `release.yml`.
- [ ] **6. Tests**: every self-test and every source invariant that names a type, file or string being renamed.
   Expect breakage here and treat each one as the guard working, not as an obstacle.
- [ ] **7. Version**: `ProductVersion.props` → `1.0.0`, `catalog.json` `app.version` → `1.0.0`.
- [ ] **8. Docs**: the sweep, plus rewriting `handoff.md` START HERE and `BACKLOG.md` around 1.0.0.

---

## Phase 3 — Verify locally, before the new repo exists

- [ ] `tests/run-gate.ps1` green under **both** `pwsh` 7 and Windows PowerShell 5.1.
- [ ] `tests/runtime-resource-soak.ps1` and `tests/module-window-soak.ps1`.
- [ ] Installer builds; `Test-MsiSurface.ps1` passes; the new UpgradeCode is present and differs.
- [ ] `Test-ModulePublishFreshness.ps1`: 12 OK, 0 stale, after republishing all seven modules.
- [ ] **The interactive test that matters most**: uninstall `DesktopPet AI Edition`, install the new MSI,
      and confirm **all six modules load**. This is the `MinHostVersion` check, and no automated test in this
      repo covers "a published module refuses the running host".
- [ ] Tray icon in the visible tray, labelled `Desktop AI Companion`; a companion spawns; `SMOKETEST.md`
      sections A through K.

---

## Phase 4 — The new repo

- [ ] Create `bigfnj/desktop-ai-companion`, **not** as a fork, default branch `master` (workflows and docs
      reference it; renaming to `main` is unrelated churn).
- [ ] Fresh single commit from the verified working tree. Confirm `.gitignore` is in place *first*, so
      `manifest/tools.json`, `build/`, `dist/`, `*.pfx` never enter history.
- [ ] Verify the initial commit contains `LICENSE`, `THIRD_PARTY_NOTICES.md`,
      `grimoire/01-history-and-lineage.md` and `docs/HISTORY-pre-1.0.0.md`.
- [ ] **Scan the initial commit for work-identifying material before pushing**, over every tracked file and
      over the commit message itself: the employer name, any work email address, and the maintainer's legal
      name. Expected result is zero hits, and the personal address as the only author identity. Not a
      formality — the first revision of this plan document leaked a work address into the public repo, and
      the scan is what found it.
- [ ] Push. Enable Actions. Grant `contents: write` for releases.
- [ ] **Set the repository description and topics.** Discovery on GitHub is topic-driven and the old repo
      had none, so it was effectively unlisted. Proposed set, accurate to what the code actually is:
      `windows`, `dotnet`, `winforms`, `wpf`, `wix`, `msi`, `shimeji`, `desktop-pet`, `desktop-companion`,
      `system-tray`, `ai`. Keeping `desktop-pet` deliberately despite the rename: it is the term people
      search for, and the whole Shimeji community indexes under it.
      **`trustedinstaller` is not on that list on purpose** — it names the Windows servicing service and
      has nothing to do with this project, so it would draw the wrong traffic and read as tag-stuffing to
      anyone who follows it. Say the word and I will add it anyway.
- [ ] **Now** verify the raw URLs resolve: `catalog.json`, one `modules-dist/*.zip`, one `packs/*.txt`.
      Regenerate the catalog if any base URL is wrong, commit, re-verify.
- [ ] Confirm `build.yml` runs green on the initial push.

---

## Phase 5 — Release v1.0.0

- [ ] Tag `v1.0.0`, push. `release.yml` must accept a 1.0.0 tag against `ProductVersion.props`.
- [ ] Verify the published assets: `sha256sum -c SHA256SUMS.txt` should now exit 0 with no preprocessing,
      since the LF fix is in this tree.
- [ ] Download the MSI **as a user**, install on a clean uninstall, walk `SMOKETEST.md`.
- [ ] Only when that passes is 1.0.0 real.

---

## Phase 6 — Retire the old repo

Last, and only after Phase 5 passes.

- [ ] Delete `bigfnj/desktopPet`.
- [ ] Remove the old `origin` remote locally; confirm `upstream` (Adrianotiger) is not a push target.
- [ ] Uninstall any leftover `DesktopPet AI Edition TESTBUILD` product.
- [ ] Delete the orphaned `%LOCALAPPDATA%\Programs\DesktopPet AI Edition` tree and the old
      `%LOCALAPPDATA%\DesktopAICompanion` data root.

---

## Parallel tracks

The core rename (Phase 2 steps 1–3) is **serial by nature**: it is one compile graph, and two agents editing
it concurrently produce conflicts that look like rename errors. What genuinely parallelises:

| Track | Work | Depends on |
|---|---|---|
| **A** | Phase 0 preservation; attribution audit | nothing |
| **B** | Docs + `grimoire/` + `templates/` prose sweep | nothing (separate files) |
| **C** | Build the Phase 1 rename map by enumerating the code | nothing |
| **D** | New repo creation and configuration (Phase 4 setup only) | nothing |
| **E** | Installer + packaging + CI (Phase 2 steps 4–5) | step 2 (exe name) |
| **F** | Verification: gate under each shell, both soaks | Phase 2 complete |

A, B, C and D run alongside the serial core. E follows the exe rename. F fans out at the end.

---

## Verification standard

Unchanged from this repo's conventions, and it matters more here than usual because a rename touches
everything and reads as harmless: the gate, source invariants via the `Get-MethodBody` slicer, in-process
self-tests behind `--flags`, and **every new or changed guard mutation-tested with the FIRED lines recorded
in the commit**. Two specific traps this repo has already paid for:

- A source invariant asserting a **bare identifier** is satisfied by a comment naming it. Assert the code
  form, on comment-stripped text.
- A mutation harness that restores with `git checkout --` destroys uncommitted work and then reports
  "FIRED" for the wrong reason. Baseline from the working tree and assert the code under test is present
  before mutating.

## Rollback

Until Phase 6, rollback is free: the old repo still exists and still serves its catalog. After Phase 6 there
is no rollback, which is the reason Phase 6 is last and gated on a passing smoke test rather than on a
passing build.
